<%= form_with model: review, url: game_reviews_path(game) do |f| %>
  <div class="field">
    <%= f.text_area :content, rows: '5', cols: '50', placeholder: 'レビュー記入欄' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
