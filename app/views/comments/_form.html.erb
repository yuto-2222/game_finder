<!--game_review_comments_path-->
<%= form_with model: [game, review, comment] do |f| %>

  <div class="field">
    <%= f.text_area :content, rows: '5', placeholder: 'コメント記入欄' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
